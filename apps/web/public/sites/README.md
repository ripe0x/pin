# /sites screenshots

The `/sites` landing page references three screenshots from this directory:

- `screenshot-index.png` — the index page of a deployed artist-page template, showing the masonry grid of auctions
- `screenshot-detail.png` — an auction detail page showing the sticky-artwork layout
- `screenshot-unfurl.png` — a Twitter/Farcaster-style mock showing how an auction link unfurls

Suggested capture target: a live deployment of the template using a real artist's address (e.g. `ripe0x.eth` from the development testing).

Until real screenshots are added here, the `<img>` tags on `/sites` will fall back to alt text and the page will still render — just without the visuals.
