# W1.3: enforce PND and artist-interface parity

> **Status: ready**
> **Owner branch: `codex/pnd-profile-reliability`**
> **Owning PR: #300**

## Outcome

PND and the independent artist template use the same live-state, quote,
transaction, reveal, and render boundaries, with CI proving that supported
releases behave identically and the artist path survives PND outages.

## Program position

- Program: artist independence and portable venue
- Workstream ID: W1.3
- Depends on: W1.2
- Unlocks: W2.2, W3.2, W4.2
- Strategy and decisions: issue #284, `docs/portable-release-boundary.md`,
  decisions 0002 and 0003

## Invariants

- `INV-TRUTH-CLASSES`
- `INV-WEB-STORED-READS`
- `INV-INDEPENDENCE-CAPABILITY`
- `INV-PORTABLE-CORE`
- `INV-COST-CEILING`
- `INV-REAL-DATA-EVIDENCE`

## Current evidence

- `@pin/release-spec`, `@pin/surface-kit`, and `@pin/surface-react` are present
  and pass their package tests.
- PND uses shared lifecycle, render, reveal, error, and fixed-price transaction
  functions, but its fixed-price React assembly still performs its own live
  reads and state composition.
- The artist template uses the direct provider and shared React state hooks for
  live release state and writes, with PND hosts blocked in an automated test.
- The template token grid pages recent sequential ids with zero per-card RPC
  reads, but it still displays explicit `No preview` placeholders because renderer-backed
  individual token reads and render assembly are not connected to the cards.
- A sequential fixture and an explicit Homage custom-adapter fixture exist in
  `@pin/surface-kit`; the generic adapter fails closed on the custom minter.
- PR #283 owns Studio management paths. `pnpm agent:overlap` reports no exact
  overlap with referenced open PRs at W1.2 completion.

## Scope

### Owned

- Move PND's generic fixed-price collection CTA to `@pin/surface-react` and the
  direct provider while retaining Postgres/editorial server composition.
- Extract any remaining assembly-specific state transforms into shared pure
  functions instead of copying them into the template.
- Connect the artist template's paged sequential token cards to bounded
  `readToken` and renderer-backed preview behavior, with typed loading,
  unavailable, failed, image, video, and HTML states.
- Add one fixture contract per supported behavior class: sequential fixed
  price and Homage custom/pooled.
- Run the same fixture expectations through the PND and artist adapters.
- Keep the PND-hostname block active for the entire artist release test and
  make it a required CI check.
- Prepare the package/template synchronization boundary, but do not publish or
  sync until every acceptance scenario passes.

### Excluded

- New venue layouts, owned by W2.2.
- Studio export, deployment, checkup, domain, or signed-site workflows, owned
  by W3.
- Feeds, embeds, and launch packs, owned by W4.
- Historical activity via direct log scans. PND keeps Postgres history and the
  artist template treats history as optional.
- Production indexer/database cutover, owned by W0.2.

## Data and dependency contract

- Inputs and truth classes: block-scoped protocol reads from the injected
  client; optional indexed/editorial context from PND providers; artist copy
  from a normalized manifest.
- Read owner: the shared direct provider owns bounded current-state reads;
  PND/Postgres owns stored history and discovery.
- Write owner: the shared provider prepares declarative writes; wagmi submits
  them in each assembly.
- Freshness and finality: visible mutable state refreshes no tighter than 12
  seconds, validation no tighter than 30 seconds, and price is requoted at the
  latest block immediately before submission.
- External services: injected RPC only for core behavior; PND enrichment is
  optional and must be blockable.
- Request-time RPC effect: one release validation set, one bounded state
  multicall, one immediate pre-write quote, and visible paged token reads.
  No history scan, wallet enumeration, or full-grid URI fan-out.
- Worker or indexer effect: none.
- Database and migration effect: none.
- Storage and recurring-cost estimate: $0.

## Design

The shared provider remains the sole protocol adapter. `@pin/surface-react`
owns async state and refresh semantics. PND and the artist template only map
the resulting typed state into their visual systems and wallet libraries.

Generic fixed-price support must validate that the configured minter points
back to the collection. Homage remains an explicit adapter with its own quote,
eligibility, selection, and transaction behavior. Unsupported custom minters
disable writes with a useful message.

Token previews remain paged and visible-only. A heavy or failed `tokenURI`
becomes a typed reduced state; renderer assembly may still produce HTML when
the required work inputs are available. No card silently swaps in unrelated or
dummy media.

## Acceptance scenarios

- Sequential fixed price: PND and artist adapters produce identical lifecycle,
  price, target, args, value, referral, reveal, and error recovery.
- Scheduled, ended, collection-capped, minter-capped, gas-only, dynamic-price,
  allowlist, wallet-cap, changed-primary-minter, and RPC-failure states fail
  closed consistently.
- Homage uses only its explicit adapter; the generic path reports unsupported.
- The artist template renders a representative live sequential token through
  its canonical renderer path, or shows the exact typed reduced reason.
- PND endpoints blocked: core template release, price, mint preparation,
  explorer links, and supported rendering continue working.
- PND provider outage: PND retains the current release action when the direct
  provider is healthy; optional history/editorial context degrades separately.
- Desktop and 390px checks pass on the PR preview and live-data local artist
  template with no broken images or page-level overflow.
- RPC call counts and refresh intervals remain inside this packet's bounded
  contract and add no recurring infrastructure cost.

## Verification receipt

- Commit SHA:
- Preview URL: `https://deploy-preview-300--art-pin.netlify.app/`
- Live data source: read-only production maglev and bounded mainnet reads
- Commands run and results:
- Routes reviewed on desktop:
- Routes reviewed at 390px:
- Degraded states reviewed:
- Performance evidence:
- RPC and recurring-cost evidence:
- Production writes performed: none
- Known limitations:
- Rollback procedure: revert W1.3 consumer changes while retaining the W1.2
  package boundary; no database or protocol rollback is required.

## Handoff

- Durable docs updated: this packet and `docs/program-state.json`
- Program state change proposed: mark W1.3 complete only after both assemblies
  and every parity/offline acceptance gate pass.
- Exact next action: refactor PND's generic fixed-price CTA onto
  `@pin/surface-react`, then connect renderer-backed token previews in the
  artist template and run the shared fixtures through both assemblies.
- Remaining blocker, if any: none.
