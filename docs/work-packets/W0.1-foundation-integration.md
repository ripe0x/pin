# W0.1: integrate the trustworthy foundation

> **Status: complete**
> **Owner branch: `codex/pnd-profile-reliability`**
> **Owning PR: #300**

## Outcome

PR #300 is reconciled once against the selected Surface UI work, passes its
full validation suite, and is ready for the documented production cutover.

## Program position

- Program: artist independence and portable venue
- Workstream: W0
- Milestone: W0.1
- Depends on: no program milestone
- Unlocks: W0.2, W1.2, W2.2, W3.2, W5.1
- Strategy: issues #284 and #301

## Invariants

- `INV-TRUTH-CLASSES`
- `INV-WEB-STORED-READS`
- `INV-INDEXER-WORKER-SPLIT`
- `INV-COST-CEILING`
- `INV-REAL-DATA-EVIDENCE`

## Current evidence

- PR #300 contains the indexer alias, ownership, attribution, availability,
  refresh, media, profile, discovery, and whole-site reliability changes.
- Its Netlify preview has been reviewed against the live maglev database.
- `pnpm agent:overlap` reports no shared files with referenced open pull
  requests after the selected Surface work was integrated and its redundant
  source PRs closed.
- The resolved overlap covered collection detail, token detail,
  `collection-onchain.ts`, indexer queries, navigation, and the capture worker.

## Scope

### Owned

- Select which active Surface UI PRs are actually shipping.
- Merge or restack those prerequisites in an explicit order.
- Rebase PR #300 once onto that result.
- Resolve every file reported by `pnpm agent:overlap` according to the owning
  issue rather than choosing one branch wholesale.
- Rerun PR #300's complete validation and live-data browser matrix.
- Produce the exact migration and deployment candidate for W0.2.

### Excluded

- New portable release packages.
- Venue redesign beyond preserving the reviewed PR #300 behavior.
- Studio independence tooling.
- Production migrations or deploys, which belong to W0.2.

## Data and dependency contract

- Read owner: web reads Postgres and the stable `indexer_live` alias.
- Write owner: Ponder owns fixed streams; worker owns long-tail and enrichment.
- Request-time RPC: no new historical or per-token scans.
- Database: migrations 025 through 035 remain ordered and additive.
- Recurring cost: target added cost is $0; optional media storage stays gated.
- Production writes: none during integration and preview verification.

## Acceptance scenarios

- All selected Surface changes survive the rebase.
- Profile artist, collector, and overlapping-role routes pass.
- Available, created, transferred, burned, and collected states pass.
- Foundation, SuperRare, Manifold, Mint, Transient Labs, and Surface coverage
  remain explicit.
- Image, video, HTML, missing-media, and loading states pass.
- Homepage, Releases, Auctions, Catalog, Studio, Preserve, guides, and dynamic
  detail routes pass on desktop and 390px mobile.
- No dummy content appears in preview evidence.
- Worker, web, indexer, migration, and cost checks pass.

## Verification receipt

Complete this section on the final rebased candidate:

### Selected Surface inputs

- PR #276 is closed and superseded by PR #300's release-led navigation,
  Postgres-only directory, editorial separation, and corrected pagination.
- PRs #277, #282, #280, #278, #279, and #281 are selected and integrated in
  that dependency order.
- Source branches remain untouched. Their commits are carried by PR #300 so
  one preview proves the connected result. PRs #277, #278, #279, #280, #281,
  and #282 were then closed as redundant integration sources.
- Conflict resolution keeps PR #300's bounded media routes after the page
  extraction through `apps/web/src/lib/collection-media-url.ts`.
- Preservation is owned by the extracted default and edition layouts instead
  of restoring the old monolithic collection page.

- Code candidate SHA: `61b0434b9c314414c7c664a4595df211278e8a9c`
- Preview URL: `https://deploy-preview-300--art-pin.netlify.app/`
- Selected prerequisite PRs and merge order: #277, #282, #280, #278, #279,
  #281. #276 superseded.
- `pnpm agent:overlap` result after reconciliation: no shared files with
  referenced open pull requests. PR #283 remains independent and did not
  overlap the integration candidate.
- Commands and results: GitHub `validate` passed; Netlify deploy, headers, and
  redirects passed; web typecheck passed; web tests passed 211/211; worker
  typecheck and production web build passed on the integrated candidate;
  `pnpm agent:check` and `git diff --check` passed.
- Desktop routes: homepage; reported artist/collector profile
  `0xcb430...17f9`; Releases page 999; Homage release; abstracts edition
  release; abstracts token #1; generic gallery; gallery page 999; empty
  minted-by filter; preservation guide; and activity API.
- 390px routes: homepage and the reported overlapping-role profile. Both had
  no page-level horizontal overflow or broken/hidden loaded images.
- Degraded states: gallery database unavailability is distinct from a true
  empty collection; a true empty owner filter says no tokens were minted by
  that address; extreme gallery and Releases pages clamp to the last valid
  page before the row query; cached profile images recover their visible
  state after hydration; collection cover and renderer media use bounded
  same-origin media routes where needed.
- Cost evidence: the gallery correction retains two bounded Postgres queries
  (count, then one clamped page) and adds no RPC, storage, provider, or
  recurring service cost. The integrated public browse paths remain
  Postgres-backed except for the documented narrow live reads.
- Known limitations: the production `indexer_live` split-brain remains the
  critical W0.2 blocker and requires a fresh versioned backfill plus guarded
  alias cutover under issue #295. The unrelated contracts CI job still fails
  its pre-existing `PooledBacked` predicted-address assertion; the application
  validation and deployed preview pass.

## Handoff

- Recorded state change: W0.1 `in_progress` to `complete`; W0.2 `proposed` to
  `ready`.
- Exact next action: execute W0.2 using the coordinated deploy order in PR #300
  and `docs/indexer-cutover.md`.
